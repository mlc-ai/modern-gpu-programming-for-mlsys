(part_data_layout)=
# Deep Dive into Data Layout

Data layout — the map from logical indices to physical placement — is the thread that ties the
GPU's memory and compute engines together. This part first builds a layout *notation* generically,
then shows the layout each GPU generation's hardware actually demands.

```{toctree}
:maxdepth: 1

../chapter_data_layout/index
../chapter_layout_generations/index
```
